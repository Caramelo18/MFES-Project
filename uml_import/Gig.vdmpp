class Gig
	instance variables
	  performing_Bands: set of (Band);
	  venue: Venue;
	  date: Utils`Date;
	
	operations
		public Gig : Venue * Utils`Date ==> Gig
		Gig(v, d) == (
			performing_Bands:= {};
			venue:= v;
			date:= d;
			return self;
		);
		
	  public  addBand : Band  ==> ()
	  addBand(b) == (
	  	performing_Bands := performing_Bands union {b};
	  )
	  pre b not in set performing_Bands
	  post b in set performing_Bands~;
	
		pure public getDate: () ==> Utils`Date
		getDate() == (
			return date;
		);
		
end Gig
