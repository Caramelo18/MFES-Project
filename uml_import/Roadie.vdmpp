class Roadie is subclass of Profile
	instance variables
	  task: Utils`Task;
	
	operations
		public Roadie : Utils`String * Utils`Task ==> Roadie
		Roadie(n, t) == (
			name:= n;
			task:= t;
			return self;
		);
		
	  public  setNewTask : Utils`Task  ==> ()
	  setNewTask(nt) ==(
	  	task := nt;
	  )
	  pre task <> nt
	  post task~ = nt;

end Roadie
