class RoadieOffer is subclass of Offer
	instance variables
	  band: Band;
	  task: Utilitys`Task;
	
	operations
	  public  RoadieOffer : Band * Utilitys`Task ==> RoadieOffer
	  RoadieOffer(b, t) == (
	  	band := b;
	  	task := t;
	  	return self;
	  );
	  
	  public getTask: () ==> Utilitys`Task
	  getTask() == (
	  	return task;
	  )
	  post RESULT = task
	  		and task = task~;
	    
end RoadieOffer
