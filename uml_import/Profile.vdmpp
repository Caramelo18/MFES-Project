class Profile
	instance variables
	  protected name: Utilitys`String;
	
	operations	
	  public  changeName : Utilitys`String  ==> ()
	  changeName(nn) == (
	  	name := nn;
	  )
	  pre name <> nn
	  post name~ = nn;

end Profile
