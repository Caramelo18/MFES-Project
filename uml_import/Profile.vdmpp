class Profile
	instance variables
	  protected name: Utils`String;
	
	operations	
	  public  changeName : Utils`String  ==> ()
	  changeName(nn) == (
	  	name := nn;
	  )
	  pre name <> nn
	  post name~ = nn;

end Profile
