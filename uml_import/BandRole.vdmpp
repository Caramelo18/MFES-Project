class BandRole is subclass of Offer
	instance variables
	  band_role_offer: Band;
	  desired_role: Utilitys`Instrument;
	
	operations
	  public  BandRole : Band * Utilitys`Instrument ==> BandRole
	  BandRole(b, d) == (
	  	band_role_offer := b;
	  	desired_role := d;
	  	return self;
	  );
	  
	  pure public getRole: () ==> Utilitys`Instrument
	  getRole() == ( 
	  	return desired_role;
	  )
	  post RESULT = desired_role
	  		and desired_role = desired_role~;

end BandRole
